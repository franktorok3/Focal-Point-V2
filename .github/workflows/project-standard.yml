name: Project Standard CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: project-standard-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  manifest:
    uses: franktorok3/Project-Setup/.github/workflows/manifest.yml@main
  application:
    uses: franktorok3/Project-Setup/.github/workflows/next-ci.yml@main
    with:
      node-version: "24"
      run-audit: true
      audit-blocking: false
      require-lint: true
      require-build: true
